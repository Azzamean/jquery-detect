version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
      - '9229:9229'
    container_name: jquerydetect
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - /app/node_modules
